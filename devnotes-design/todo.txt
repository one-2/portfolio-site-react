* backend integration with MongoDB Atlas:
  + build+test database for user contacts and email signups
  + integrate database api calls into frontend
  + set up MDB trigger to email me new submissions. ideally once per day.
  + set up github secrets. (must be done before prod build)
  + put github back on public

* v3 interface
  + implement interface from my notebook
  + contact terminal overlay via Next Layouts
  + improve json structure (low pri)
  + update readme (low pri)

* art gallery  (new branch for this)
