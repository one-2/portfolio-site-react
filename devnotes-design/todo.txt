* backend integration with MongoDB Atlas:
  + build tool to extract records from the db 
  + set up github secrets. must be done before prod build. (high pri)

* v3 interface
  + implement interface from my notebook
  + contact terminal overlay via Next Layouts
  + improve json structure (low pri)
  + update readme (low pri)

* art gallery  (new branch for this)
